package fr.flegac.jstory.stories.other_Features_Exemple.administrator;

import fr.flegac.jstory.annotations.Epic;
import fr.flegac.jstory.annotations.Step;

@Epic(why = "allow a new user to work with the web application",
      who = "administrator",
      what = "create a new login/password for the new user")
@Step(when = "Open web site",
      then = "")
@Step(when = "click on login",
      then = "")
@Step(when = "enter user/password",
      then = "")
@Step(when = "click ok",
      then = "")
public class AdminTest {

}
